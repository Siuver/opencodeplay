# OpenCode Agent Templates

Place project-local OpenCode agent definitions here. The doctor verifies this directory exists, but bootstrap should copy files only when missing unless an ownership marker proves the target is repo-managed.

Recommended target:

```text
.opencode/agents/
```

Do not put provider credentials, API keys, or personal tokens in agent templates.
