# OpenCode Plugin Templates

Place local OpenCode plugin templates here only when they can be reviewed safely. Plugin installation can download dependencies through Bun, so plugin assets must remain approval-gated unless the manifest explicitly proves no network or dependency installation is required.
