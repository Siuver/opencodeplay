# Doctor

Run the repository doctor and summarize the result.

```powershell
.\scripts\doctor.ps1 -Profile default
```

If failures appear, fix schema, manifest reference, safety invariant, or template source issues before attempting setup actions.
