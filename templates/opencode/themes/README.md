# OpenCode Theme Templates

Place project-local OpenCode theme files here. Theme assets are optional and should be copied only when missing unless the target directory contains `.opencode-bootstrap-owned.json`.
