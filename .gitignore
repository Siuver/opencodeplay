/.opencodeplay/
/tmp/

# Offline artifacts can be large. Keep the directory contract, but commit files
# only intentionally or through release bundles.
/artifacts/*
!/artifacts/README.md
!/artifacts/.gitkeep

# Local generated reports.
/logs/
*.log
