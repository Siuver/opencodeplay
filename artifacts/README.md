# Offline Artifacts

Place pinned archives, binaries, package caches, or source snapshots here when preparing an offline bundle.

The file names must match `manifests/tools.json`. Large files are ignored by default in `.gitignore`; include them only in private mirrors or release bundles when that is intentional.
